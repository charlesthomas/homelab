apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    external-dns.alpha.kubernetes.io/hostname: traefik.crt.house
  name: traefik-dash
  namespace: traefik
spec:
  ingressClassName: traefik-internal
  rules:
  - host: traefik.crt.house
    http:
      paths:
      - backend:
          service:
            name: traefik
            port:
              number: 9000
        path: /
        pathType: Prefix
status:
  loadBalancer: {}
